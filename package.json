{
  "name": "bun-htmx-starter",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun build.js --watch",
    "build": "bun build.js",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "htmx.org": "^2.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}